import React from 'react';
import { CaptureManager } from '../components/capture';

const CaptureManagerPage = () => {
  return (
    <div className="capture-manager-page">
      <CaptureManager />
    </div>
  );
};

export default CaptureManagerPage;